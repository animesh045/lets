<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Sunshine Workshop – Admin Login</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <%- include('partials/header') %>
  <main class="container">
    <h1>Admin Login</h1>
    <% if (error) { %>
      <div class="alert error"><%= error %></div>
    <% } %>
    <form action="/admin/login" method="POST" class="card gradient">
      <label for="pin">PIN</label>
      <input id="pin" name="pin" type="password" placeholder="Enter admin PIN" required />
      <button type="submit">Login</button>
    </form>
    <p class="muted mt"><a href="/">← Back to Registration</a></p>
  </main>
</body>
</html>
